import React from "react";
import { RingLoader } from "react-spinners";

const Spinner = () => (
  <div className="spinner">
    <RingLoader color={"#F0E3C0"} size={90} />
  </div>
);

export default Spinner;
